import LetTalkLayout from "@/layouts/let-talk/layout";
import React from "react";

export default function layout({ children }: { children: React.ReactNode }) {
  return <LetTalkLayout>{children}</LetTalkLayout>;
}
